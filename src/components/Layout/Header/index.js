import React from 'react';

import { StyledRoot } from './styled';


export default function Header(props) {
    return (
        <StyledRoot>
            {/* Action open drawer */}
        </StyledRoot>
    );
};
